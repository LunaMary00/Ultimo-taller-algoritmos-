public class ValidadordeContrasenas {

    // definir variables

    public class void main (String [] args) { 

        Sistem.out.println ("=== ValidadordeContraseñas")

        Sistem.out.println ("12345 " + evaluarContrasena ("12345") )
        Sistem.out.println (" sololetras " + evaluarContrasena ("sololetras") )
        Sistem.out.println ("Clave123 " + evaluarContrasena ("Clave123") )
        Sistem.out.println (" Admin2026 " + evaluarContrasena ("Admin2026") )
        Sistem.out.println (" Prog*2026!" + evaluarContrasena ("Prog*2026!") )
}
 
plubic static boolean tieneLongitudMinima (String pass, int min) { return pass.length() >=min }

public static boolean tieneMayuscula (String pass) { 
    for (char c: pass.toCharArray ()) { 
        if (Character.isUpperCase(c)) { 
            return true;
            }
            }

            return false;
}

public static boolean tieneDigito (String pass) { 
for (char c: pass.toCharArray()){ 
    if (Character.isDigir(c)){ 
        return true;
    }
    }   

    return false;

    }

public static tieneCaracterEspecial (String pass) {
    String especiales = "!@#$%&*"
   for (char c: pass.toCharArray ()) {
    if (especiales.contais(String.ValueOf(c))) 
        return true;
    }
    }

    return false;

plubic static boolean String evaluarContrasena(String pass) { 
    int longitudMinimaRequerida = 8 

    if (!tieneLongitudMinima (pass, longitudMinimaRequerida)){ 
        return "Débil";

        int reglasCumplidas = 0;

if (tieneMayuscula (pass)) reglasCumplidas++;
if (tieneMninuscula (pass)) reglasCumplidas++;
if (tieneDigito (pass)) reglasCumplidas++;
if (tieneCaracterEspecial (pass)) reglasCumplidas++;

if (reglasCumplidas == 4){ 
return "Fuerte";

else if (reglasCumplidas >= 2){ 
    return "Media";

else { 
    return " Debil";

}
}
}
}


